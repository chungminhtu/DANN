@{
	var treeList = Html.DevExpress().TreeList(settings => {
        settings.Name = "ChiTieuTreeList";
        settings.CallbackRouteValues = new { Controller = "TKChiTieu", Action = "ChiTieuTree" };

				settings.SettingsEditing.AddNewNodeRouteValues = new { Controller = "TKChiTieu", Action = "ChiTieuTreeAddNew" };
        settings.SettingsEditing.UpdateNodeRouteValues = new { Controller = "TKChiTieu", Action = "ChiTieuTreeUpdate" };
        settings.SettingsEditing.DeleteNodeRouteValues = new { Controller = "TKChiTieu", Action = "ChiTieuTreeDelete" };
				settings.SettingsEditing.NodeDragDropRouteValues = new { Controller = "TKChiTieu", Action = "ChiTieuTreeMove" };

		settings.CommandColumn.Visible = true;
        settings.CommandColumn.NewButton.Visible = true;
        settings.CommandColumn.DeleteButton.Visible = true;
        settings.CommandColumn.EditButton.Visible = true;

				settings.KeyFieldName = "Id";
		settings.ParentFieldName = "ParentId";
				settings.Columns.Add(
            column => {
                column.FieldName = "TenChiTieu";
            }
        );
				settings.Columns.Add(
            column => {
                column.FieldName = "Id";
            }
        );
				settings.Columns.Add(
            column => {
                column.FieldName = "Id";
            }
        );

		settings.SettingsPager.Visible = true;
		settings.SettingsSelection.Enabled = true;
	});
		if (ViewData["EditError"] != null){
        treeList.SetEditErrorText((string)ViewData["EditError"]);
    }
}
@treeList.Bind(Model).GetHtml()